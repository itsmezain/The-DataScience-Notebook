
		MultiIndex Series
-----------------------------------------------------------------------
1- Create multiindex object:
	a) pd.MultiIndex.from_tuples()
	b) pd.MultiIndex.from_product()
2- Levels inside miltindex object
3- how to fetch items from such a series

4- What is the need of the Multiindex object?
5- DataFrame where columns have hierarchy
6- Multiindex in both row and columns
7- stack() and unstack()
8- Working with multiindex dataframes:
	(a) head and tail
	(b) shape
	(c) info()
	(d) duplicated() and isnull()

9- Extracting  single and mutiple rows using loc & iloc
10- Extracting single and mutiple column using loc & iloc
11- Sort index -> different ways to sort multindex dataframe (4d)-> based on one level
12- Long vs Wide Data
13 - melt()
14- real world dataset